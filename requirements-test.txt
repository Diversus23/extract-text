# Минимальные зависимости для тестирования
# Версии runtime-пакетов синхронизированы с requirements.txt (== вместо >=).
# В CI/Docker устанавливаются вместе с requirements.txt — порядок не важен,
# pip разрешит совместимость.

# Веб-фреймворк и сервер
fastapi==0.136.1
uvicorn[standard]==0.46.0

# Обработка файлов
python-multipart==0.0.27

# PDF обработка (основные пакеты)
PyPDF2==3.0.1
pdfplumber==0.11.9

# Word и Office документы
python-docx==1.2.0
python-pptx==1.0.2

# RTF документы
striprtf==0.0.32

# Таблицы
pandas==3.0.2
openpyxl==3.1.5

# Изображения (без OCR для начала)
Pillow==12.2.0

# HTML и Markdown
# 4.13.5 — последняя в 4.13.x; 4.14 несовместим с extract-msg<0.56.
beautifulsoup4==4.13.5
lxml==6.1.0
markdown==3.10.2

# YAML и XML
PyYAML==6.0.3
defusedxml==0.7.1

# Общие утилиты
python-dotenv==1.2.2
werkzeug==3.1.8

# Логирование
structlog==25.5.0

# Тестирование
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
httpx==0.28.1
pytest-mock==3.15.1
requests==2.33.1
